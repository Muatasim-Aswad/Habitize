import React from "react";
import { Box } from "@mui/material";

const AddHabit = () => {
  return (
    <Box
      sx={{
        flex: 1,
        display: "flex",
        flexDirection: "column",
      }}
    />
  );
};

export default AddHabit;
